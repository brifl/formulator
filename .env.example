# Required API key.
OPENAI_API_KEY=

# Use models that support the Chat Completions endpoint (/v1/chat/completions).
# Examples (not exhaustive): gpt-5.2, gpt-5, gpt-5-mini, gpt-4.1, gpt-4.1-mini, gpt-4o, gpt-4o-mini
# Account access varies by org/project; verify with GET /v1/models.
PREMIUM_LLM_MODEL=
BUDGET_LLM_MODEL=

# Optional: leave blank to omit reasoning_effort from requests.
# Valid values: none, minimal, low, medium, high, xhigh
PREMIUM_LLM_REASONING_EFFORT=
BUDGET_LLM_REASONING_EFFORT=

# Optional phase temperatures; leave blank to use app defaults.
ADD_LLM_TEMP=
RED_LLM_TEMP=

# Optional verbose request/response logging for troubleshooting.
# WARNING: when enabled, prompt and response bodies are logged to logs/app.log.
VERBOSE_LLM_LOGGING=
# Optional max chars for each logged prompt/response body (must be >= 100, default 4000).
VERBOSE_LLM_LOG_MAX_CHARS=
